Reinforcement learning tutorial using the MuJoCo cart–pole: open-loop control, RL setup, reward design, training (PPO), and reward hacking demos.

## Viewing the tutorial

Videos won't load if you open `index.html` directly (file://). Serve it over HTTP:

```bash
./serve_tutorial.sh
```

Then open http://localhost:8000/index.html in your browser.